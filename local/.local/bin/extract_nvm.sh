[[ -v NVM_DIR ]] && exit 2  || pre=$(env); [ `alias | grep nvm | wc -l` != 0 ] && unalias nvm; export NVM_DIR="$HOME/.nvm"; [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"; post=$(env); res=$(diff <(echo "$pre") <(echo "$post") | grep "^>" | sed "s/^>\s//"); echo "$res";
